git add <file1> <file2>
git commit –m "<Type your commit message here>"
git push


git checkout -b <branch-name> //creates a new branch an go into it
git checkout <branch-name> //Switch into branch-name

git merge <name-of-branch-to-merge-in> // You are already in main with checkout main

git stash push <file1> <file2>
git pull